# 轨迹平滑导致楼梯处水平化问题分析

## 问题描述
在楼梯下降段，优化后的轨迹完全水平，没有跟随地面下降，高度误差达到3m。

## 根本原因

### 1. 下界约束过于宽松
**位置**：`height_smoother.cc` 第22行

```cpp
lbs.emplace_back(-1.0);  // 下界固定为-1.0，太宽松！
```

**问题**：所有点的下界都是-1.0，允许轨迹高度在很大范围内变化，没有强制要求贴合地面。

### 2. 平滑权重远大于参考线权重
**位置**：`height_smoother.cc` 第32-33行

```cpp
kernel->AddThirdOrderDerivativeMatrix(30);      // 平滑项权重 = 30
kernel->AddReferenceLineKernelMatrix(ts, refs, 1);  // 参考线权重 = 1
```

**问题**：优化器优先考虑"平滑性"（最小化三阶导数），而不是跟随A*原始路径。在楼梯这种大高度变化区域，为了平滑性会严重偏离原始路径。

### 3. 优化目标不平衡
HeightSmoother的优化目标是：

```
minimize: 30 * (三阶导数)^2 + 1 * (高度 - 参考高度)^2
subject to: -1.0 <= 高度 <= upper_bound - 0.3
```

在楼梯处，如果保持跟随A*路径（高度变化大），三阶导数会很大，代价也大。
如果变成水平（高度不变），三阶导数接近0，虽然偏离参考线，但总代价可能更小。

**30:1的权重比导致优化器选择了"平滑但偏离"的解。**

## 解决方案

### 方案1：增加参考线权重（推荐）
修改`height_smoother.cc`第33行：

```cpp
// 从权重1提高到50或100
kernel->AddReferenceLineKernelMatrix(ts, refs, 50);  // 或100
```

**效果**：强制优化器更紧密跟随A*原始路径，减少在楼梯处的偏离。

### 方案2：使用地面高度作为下界
修改`height_smoother.cc`第22行：

```cpp
// 使用地面高度作为下界，而不是固定的-1.0
// 需要传入ground_height参数
lbs.emplace_back(ground_height(i));  
```

**效果**：强制轨迹高度不能低于地面，防止过度平滑导致穿地或浮空。

### 方案3：降低平滑权重
修改`height_smoother.cc`第32行：

```cpp
// 从权重30降低到5-10
kernel->AddThirdOrderDerivativeMatrix(10);
```

**效果**：允许轨迹有更大的曲率变化，适应楼梯等地形。

### 方案4：自适应权重（最佳但复杂）
根据路径段的高度变化自适应调整权重：

```cpp
double height_change = std::abs(coarse_height(i) - coarse_height(i-1));
double ref_weight = height_change > 0.5 ? 50.0 : 1.0;  // 楼梯处增加参考线权重
```

## 推荐修改方案

### 快速修复（简单有效）
修改`height_smoother.cc`：

```cpp
// 第22行：使用实际地面高度（如果可获取）
lbs.emplace_back(coarse_height(i) - 0.5);  // 允许最多下降0.5m

// 第33行：大幅增加参考线权重
kernel->AddReferenceLineKernelMatrix(ts, refs, 100);  // 从1提高到100
```

### 完整修复（需要修改接口）
1. 修改`HeightSmoother::Smooth`接口，添加`lower_bound`参数
2. 传入实际地面高度作为下界
3. 根据高度变化自适应调整平滑和参考线权重

## 为什么A*路径正确但优化后错误？

1. **A*阶段**：选择有效的地面节点，高度完全跟随tomogram ✓
2. **GPMP优化阶段**：只优化XY位置和速度，高度从A*继承 ✓
3. **高度平滑阶段**：重新优化高度，此时问题出现 ✗

HeightSmoother在第178行被调用：
```cpp
opt_height_ = height_smoother_.Smooth(opt_height_, opt_ceiling_, tau, N, dt);
```

这一步破坏了A*路径的正确高度！

