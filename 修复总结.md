# A*轨迹浮空问题修复总结

## 问题原因

使用`567_-ready-to-pctplanner.ply`点云规划的轨迹有浮空现象。

### 根本原因
A*在搜索时使用了**没有有效地面数据**的节点（elev_g为NaN，被转换为-100）。这些节点位于某些slice的某些位置，导致规划的路径浮空。

#### tomogram数据特征
1. 共9个slice（layer 0-8）
2. 相邻slice之间高度不连续，很多位置高度差>0.3m，最大达12.77m
3. 许多位置在某些slice没有有效地面数据（NaN）

## 修复方案

### 1. 添加节点有效性标记
在`a_star_search.h`的Node类中添加`bool valid`标记：
- 在A*初始化时，根据height值设置valid标志（height > -50.0表示有效）
- 无效节点不会被添加到搜索路径中

### 2. 多处检查节点有效性
在`a_star_search.cc`的关键位置添加valid检查：
- **主搜索循环（第110行）**：跳过无效的current_node
- **邻居扩展（第177行）**：跳过无效的neighbor_node  
- **DecideLayer函数（第239-303行）**：只在有效层之间切换

### 3. 放宽高度约束
- 将邻居节点高度差约束从0.3m提高到2.0m
- 将DecideLayer高度差约束从0.2m提高到2.0m
- 允许A*在相邻slice之间更灵活地切换

## 修复效果

### A*原始路径（修复前 vs 修复后）
| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 平均偏差 | 82.473m | **0.000m** ✓ |
| 最大偏差 | 103.100m | **0.000m** ✓ |
| 浮空点数 (>0.5m) | 80.0% | **0.0%** ✓ |

### 优化后轨迹（整体效果）
| 指标 | 数值 |
|------|------|
| 轨迹点数 | 415 |
| Z高度范围 | [-1.10, 0.24] m |
| 平均偏差 | 0.337m |
| 最大偏差 | 3.236m |
| 浮空点数 | 87/415 (21.0%) |

## 结论

✅ **A*路径规划问题已完全解决** - 原始路径100%贴合地面

⚠️ **轨迹优化阶段仍有21%浮空** - 这是因为GPMP优化器在平滑轨迹时会偏离原始A*路径

## 建议

### 如果对当前结果满意（21%浮空可接受）
直接使用，A*核心问题已解决。

### 如果需要进一步改进
可以调整轨迹优化参数：
1. **增加地面约束权重**：在GPMP优化器中增加对地面高度的约束
2. **减小平滑程度**：降低轨迹平滑的激进程度
3. **后处理投影**：将优化后的轨迹投影回最近的有效地面

## 修改的文件

### C++代码
- `planner/lib/src/a_star/a_star_search.h` - 添加valid标记
- `planner/lib/src/a_star/a_star_search.cc` - 添加有效性检查和放宽高度约束

### Python脚本
- `planner/scripts/planner_wrapper.py` - 改进findValidSlice逻辑
- `planner/scripts/diagnose_floating.py` - 诊断脚本
- `planner/scripts/test_plan.py` - 测试脚本
- `planner/scripts/check_astar_raw.py` - A*原始路径检查脚本

## 如何使用

```bash
# 1. 重新编译（已完成）
cd /home/cjsg/PCT_planner/planner
bash build.sh

# 2. 运行规划
cd scripts
conda activate go2w
python plan.py --scene sii_l6

# 3. 检查效果
python check_astar_raw.py  # 查看A*原始路径
python test_plan.py        # 查看完整轨迹
```

## 技术细节

### 为什么A*原始路径会使用无效节点？
1. tomogram某些位置在某些slice没有有效地面数据（NaN）
2. A*初始化时，这些位置的height被设置为-100（从NaN转换）
3. 修复前，A*会将这些无效节点加入搜索，导致路径浮空

### 为什么优化后轨迹仍有浮空？
GPMP优化器使用轨迹优化算法平滑路径，目标是：
- 最小化加速度（平滑性）
- 避开障碍物
- 满足动力学约束

但**没有强约束要求轨迹紧贴地面**，所以优化过程中会偏离原始A*路径。

### 如何进一步改进？
需要修改GPMP优化器，添加地面高度约束：
```cpp
// 在gpmp_optimizer.cc中添加地面约束项
cost += weight_ground * (traj_height - ground_height)^2
```

